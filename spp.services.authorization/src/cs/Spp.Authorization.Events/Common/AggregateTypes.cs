namespace Spp.Authorization.Events.Common;

public static class AggregateTypes
{
    public const string GeneratedUserNamePart = nameof(GeneratedUserNamePart);

    public const string PermissionGroup = nameof(PermissionGroup);

    public const string Role = nameof(Role);

    public const string User = nameof(User);
}
